/* eslint-disable @next/next/no-html-link-for-pages */
"use client";
import { useState } from "react";
import { canCapture, SCOPA_HAND, SCOPA_TABLE } from "../../../../lib/scopa";
export default function ScopaPlayPage() { const [selected, setSelected] = useState<number | null>(null); const [step, setStep] = useState(1); const selectedValue = selected === null ? null : SCOPA_HAND[selected]; const valid = selectedValue !== null && canCapture(SCOPA_TABLE, selectedValue); const instruction = step === 1 ? selected === null ? "从手牌中选择一张牌" : valid ? "很好！这张牌可以捕获桌面组合。" : "这张牌暂时没有可捕获的组合。" : step === 2 ? "捕获后，牌会从桌面移到你的牌堆。" : "完成！你已经掌握 Scopa 的基本捕获规则。"; return <main className="play-page"><nav className="nav"><a className="brand" href="/"><span className="brand-mark">✦</span> WORLD CARD <small>ATLAS</small></a><a className="back-link" href="/games/scopa">← 返回 Scopa 档案</a></nav><section className="play-shell"><div className="play-intro"><p className="eyebrow">SCOPA · INTERACTIVE TUTORIAL</p><h1>三分钟，<br /><em>学会 Scopa。</em></h1><p>选择一张手牌，理解 Scopa 的核心捕获规则。</p><div className="rule-list"><div><b>01</b><span>从手牌中选择一张牌</span></div><div><b>02</b><span>捕获点数相加等于它的牌</span></div><div><b>03</b><span>捕获最多牌，赢得胜利</span></div></div></div><div className="table" aria-live="polite"><div className="table-top"><span>SCOPA · 新手桌</span><span>第 {step} / 3 步</span></div><div className="instruction">{instruction}<small>目标：捕获桌面上的牌。</small></div><div className="table-cards center">{SCOPA_TABLE.map((x, i) => <div key={i} className={`playing-card mini ${i === 0 ? "red" : ""}`}>{x === 1 ? "A" : x}</div>)}</div><div className="table-cards hand">{SCOPA_HAND.map((x, i) => <button aria-label={`选择 ${x === 1 ? "A" : x} 点牌`} key={i} className={`playing-card ${canCapture(SCOPA_TABLE, x) ? "glow" : ""} ${selected === i ? "picked" : ""}`} onClick={() => setSelected(i)}>{x === 1 ? "A" : x}<small>♠</small></button>)}</div>{step < 3 && ((step === 1 && valid) || step === 2) && <div className="hero-actions"><button className="btn primary" onClick={() => setStep(step + 1)}>{step === 1 ? "进入第 2 步 →" : "完成教程 →"}</button></div>}</div></section></main>; }
