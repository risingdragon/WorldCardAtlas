# World Card Atlas UI Modernization Contract

> 版本：v1.0 · 2026-08-07  
> 状态：待人工选择视觉方向  
> 范围：仅 UI 表现层；不改业务逻辑

## 1. 不可变边界

本轮只允许增量修改 `app/globals.css`，必要时补充最小化的可访问性属性样式；不改动 `app/page.tsx`、`app/play/page.tsx`、路由、接口、状态、事件处理、筛选规则、试玩计分逻辑、页面文案和跳转行为。

必须继续保留：

- 首页 `/` 的导航锚点、探索地图、地区筛选、游戏库搜索/筛选、游戏详情弹窗和试玩入口。
- 试玩页 `/play` 的返回路径、选牌反馈、可捕获提示、得分累加和三步教程。
- 现有中英文文案与 `?game=` 跳转参数。

## 2. 现状判断

当前风格有明确的“世界纸牌档案”概念：深墨绿、奶油底、荧光黄、纸牌/地图视觉隐喻。主要现代化机会：

- 目前大量样式压缩在单行 CSS 中，字号和间距缺少统一 token。
- `2px` 按钮圆角、纯色大块和较重的装饰字容易产生廉价模板感。
- 输入框、选择器、弹窗、卡牌按钮缺少完整的 focus / disabled / active 状态。
- 移动端已有基础断点，但导航、筛选控件、弹窗内容与试玩桌仍需更稳定的纵向节奏和触控尺寸。

## 3. 三套候选视觉方向

### A · 简约商务（推荐）

定位：像一份编辑精良的文化档案与精品桌游目录。保留现有深墨绿资产，但降低荧光感，强化白空间、信息层级与细边框。

- 色彩：Ink `#14211D`、Canvas `#F7F5EF`、Surface `#FFFFFF`、Accent `#C9D83E`、Muted `#59665E`、Line `#DDE2DB`。Muted 文字需满足浅色背景 AA 对比度。
- 形状：卡片 `12px`，弹窗 `16px`，按钮 `8px`；避免全站胶囊化。
- 阴影：`0 8px 24px rgba(20,33,29,.08)`，仅用于悬浮层和可点击卡片。
- 排版：无衬线承担 UI 与正文；衬线只保留在品牌强调词和卡牌数字。
- 动效：卡片上浮 `translateY(-3px)`，按钮轻微亮度变化，150–200ms ease-out。
- 适用：希望可信、耐看、适合长期扩充游戏档案时选择。

### B · 现代轻量

定位：更年轻、更有空气感的文化探索产品。保留卡牌的彩色记忆点，用柔和背景和轻边框降低沉重感。

- 色彩：Ink `#1C2925`、Canvas `#F2F6F3`、Surface `#FFFFFF`、Accent `#B7D63B`、Sky `#DCEEF0`、Warm `#F6E7C9`、Line `#D9E4DE`。
- 形状：卡片 `18px`，模块 `24px`，按钮 `999px`；仅操作控件使用胶囊。
- 阴影：`0 12px 30px rgba(31,59,48,.09)`，配合很浅的渐变背景。
- 排版：标题采用紧凑大字号，正文 15–16px；标签使用较少的全大写英文。
- 动效：地图点位与卡片 hover 有 180ms 位移/缩放，避免持续漂浮动画。
- 适用：希望更亲和、更接近现代消费级产品时选择。

### C · 干净 SaaS 风

定位：把“世界纸牌档案”做成清晰、高效、可检索的产品。强调表单、筛选、状态和可用性，装饰只做辅助。

- 色彩：Ink `#182321`、Canvas `#F8FAF9`、Surface `#FFFFFF`、Accent `#A8C52A`、Primary `#245E4B`、Muted `#66746D`、Line `#E2E8E4`。
- 形状：控件 `8px`，卡片 `12px`，弹窗 `14px`；统一 1px 边框。
- 阴影：默认无阴影，hover `0 4px 14px rgba(24,35,33,.08)`，突出层级而不是装饰。
- 排版：正文 14–16px、标题 40–64px；信息卡使用 tabular 数字与更清晰的 metadata。
- 动效：优先 focus ring、loading/disabled 反馈和键盘可达性；hover 仅做颜色与 2px 位移。
- 适用：希望游戏库未来扩展更多筛选、数据和内容时选择。

## 4. 共用设计 token

实现时将 token 放在 `:root`，由选定方案覆盖颜色 token；组件选择器继续沿用现有 class，避免改写 JSX。

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --space-6: 32px;
  --space-7: 48px;
  --space-8: 64px;
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --focus-ring: 0 0 0 3px color-mix(in srgb, var(--accent) 42%, transparent);
  --ease-standard: 180ms cubic-bezier(.2,.8,.2,1);
  --text-xs: 11px;
  --text-sm: 13px;
  --text-md: 15px;
  --text-lg: 18px;
  --text-xl: 28px;
}
```

## 5. 状态契约

- Hover：所有可点击卡片、按钮、导航链接均有明确颜色或层级变化；不依赖颜色单一表达。
- Focus-visible：按钮、链接、输入框、选择器、卡牌按钮显示 `--focus-ring`，不能移除浏览器可见焦点。
- Active/selected：保留现有 `.chosen`、`.picked`、`.glow` 语义，增强背景、边框或 outline，不改变状态判断。
- Disabled：视觉上降低至约 50% 不透明度，移除阴影和 hover；若现有组件没有 disabled 分支，只提供通用 CSS，不新增业务状态。
- Modal：打开时背景遮罩保持清晰对比；关闭按钮触控区域不小于 `44px`。
- Reduced motion：`@media (prefers-reduced-motion: reduce)` 下关闭位移与平滑滚动动画。

## 6. 响应式契约

- `> 1100px`：首页 hero 双栏、地图双栏、游戏库三列；内容最大宽度建议 `1180px`。
- `801–1100px`：hero 艺术区域缩小，游戏库两列，地图保持双栏但减少内边距。
- `≤ 800px`：导航链接收起；hero、试玩区改为单列；游戏卡单列；筛选控件纵向堆叠；地图列表置于地图下方。
- `≤ 480px`：页面左右内边距 `20px`；主按钮至少 `44px` 高；标题使用 `clamp()`，避免横向溢出；弹窗内边距 `20px`。
- 所有可点击目标最小触控尺寸 `44×44px`；正文行高不低于 `1.5`。

## 7. 选择后的增量实施顺序

1. 只在 `app/globals.css` 建立 token 与选定配色。
2. 先调整全局排版、容器、按钮/表单和 card 基础样式。
3. 再处理首页 hero、地图、游戏库、modal。
4. 最后处理 `/play` 试玩桌、状态和移动端断点。
5. 运行 lint/build，并逐页核验原有搜索、筛选、弹窗、跳转、选牌、计分和步骤推进。

## 8. 当前等待选择

请从 A「简约商务」、B「现代轻量」、C「干净 SaaS 风」中选择一个。收到选择后，我将只做对应方案的 CSS 增量改造，并保留全部业务代码不动。
